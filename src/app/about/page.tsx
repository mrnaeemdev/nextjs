import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "about page",
  description: "This is the about page of website",
};

const About = () => {
  return <h1>About page</h1>;
};

export default About;
